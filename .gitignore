# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment / secrets
.env
.env.*
!.env.example

node_modules
dist
dist-electron
backups
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# SQLite databases
*.sqlite
*.db
*.sqlite-journal
*.sqlite-shm
*.sqlite-wal

# Fichier comptable actif / dossiers récents (état local par poste, jamais du code source)
server/active-db-config.json
server/db-registry.json
server/public/exports/

# Installer build artifacts (rebuilt locally, not source)
installer-output/
packaging/node-v24.15.0-win-x64/
packaging/stage/
packaging/node-portable.zip
packaging/*.log
packaging/*.flag

# LibreOffice portable (conversion PDF de la DSF) : binaire volumineux, jamais versionné,
# déposé manuellement en local pour empaqueter l'installateur (voir server/pdfConvert.js)
packaging/libreoffice-portable/

